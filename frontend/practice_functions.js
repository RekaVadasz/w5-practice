function multiply(number1, number2) {
    return number1 * number2;   // The function returns the product of p1 and p2
  }
  
console.log(multiply(3,5))

for (const item of arr) {
    console.log(item)
}
